2. Inkapsling
Skapa en klass BankAccount med:
Ett privat fält balance.
Metoder: Deposit(amount) och Withdraw(amount) som uppdaterar saldot på ett säkert sätt.
En metod GetBalance() som visar saldot.
Testa i Main med insättningar och uttag.